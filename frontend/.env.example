# Leave this blank/unset for local development — the Vite dev server proxies
# /api requests to the backend automatically (see vite.config.js), so no
# CORS configuration is needed.
#
# When building for production, set this to your deployed backend's full URL:
# VITE_API_URL=https://api.your-domain.com/api
VITE_API_URL=
