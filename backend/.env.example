# Server
PORT=5000
NODE_ENV=development

# Database (SQLite file path, relative to backend/)
DB_STORAGE=./database/e_giciro.sqlite

# Auth
JWT_SECRET=change_this_to_a_long_random_string_before_deploying
JWT_EXPIRES_IN=7d

# CORS
CLIENT_ORIGIN=http://localhost:5173
